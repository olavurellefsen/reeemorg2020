import React from "react"
import Layout from "../components/layout"
import SEO from "../components/seo"

export default (props) => (
  <Layout pageTitle="NEWAGE" location={props.location} showSocialMedia={['top', 'bottom']}>
    <SEO title="NEWAGE" />
    <p>
Lorum ipsum
    </p>
  </Layout>
)
